Great! Now that **Feature 00: Preparation** is complete, I’ll proceed with **Feature 01: User Model**. This feature involves creating a user model with validation, generating realistic user data using a library like `faker`, and adding logging and unit tests.

---

### **Feature 01: User Model**

#### **1. User Model**
The user model will include the following fields:
- `id`: UUID (generated by the mocking service).
- `firstName`: Non-empty and no longer than 255 characters.
- `lastName`: Non-empty and no longer than 255 characters.
- `age`: Generated in a real-life range (e.g., 18–100).
- `position`: Generated using a library like `faker`.

##### **`src/main/scala/com/example/model/User.scala`**
```scala
package com.example.model

import java.util.UUID

case class User(
  id: UUID,
  firstName: String,
  lastName: String,
  age: Int,
  position: String
)

object User {
  // Validation for firstName, lastName, and position
  def validateName(name: String): Option[String] = {
    if (name.nonEmpty && name.length <= 255) Some(name) else None
  }

  // Validation for age
  def validateAge(age: Int): Option[Int] = {
    if (age >= 18 && age <= 100) Some(age) else None
  }
}
```

---

#### **2. User Generation**
I’ll use the `faker` library to generate realistic user data. For Scala, a popular library is `scala-faker`.

##### **Add Dependency to `build.sbt`**
```scala
libraryDependencies += "com.github.javafaker" % "javafaker" % "1.0.2"
```

##### **`src/main/scala/com/example/util/UserGenerator.scala`**
```scala
package com.example.util

import com.github.javafaker.Faker
import com.example.model.User
import java.util.UUID

object UserGenerator {
  private val faker = new Faker()

  def generateUser(): User = {
    User(
      id = UUID.randomUUID(),
      firstName = faker.name().firstName(),
      lastName = faker.name().lastName(),
      age = faker.number().numberBetween(18, 100),
      position = faker.job().title()
    )
  }

  def generateUsers(count: Int): List[User] = {
    (1 to count).map(_ => generateUser()).toList
  }
}
```

---

#### **3. Logging**
I’ll use **Logback** with **SLF4J** for logging.

##### **Add Dependency to `build.sbt`**
```scala
libraryDependencies += "ch.qos.logback" % "logback-classic" % "1.4.7"
```

##### **`src/main/scala/com/example/util/Logger.scala`**
```scala
package com.example.util

import org.slf4j.{Logger, LoggerFactory}

trait Logger {
  protected val logger: Logger = LoggerFactory.getLogger(getClass)
}
```

##### **Example Usage in `UserGenerator`**
```scala
import com.example.util.Logger

object UserGenerator extends Logger {
  // ...

  def generateUsers(count: Int): List[User] = {
    logger.info(s"Generating $count users...")
    val users = (1 to count).map(_ => generateUser()).toList
    logger.info(s"Generated ${users.size} users.")
    users
  }
}
```

---

#### **4. Unit Tests**
I’ll write unit tests for the user model and user generation.

##### **Add Dependency to `build.sbt`**
```scala
libraryDependencies += "org.scalatest" %% "scalatest" % "3.2.15" % Test
```

##### **`src/test/scala/com/example/model/UserTest.scala`**
```scala
package com.example.model

import org.scalatest.flatspec.AnyFlatSpec
import org.scalatest.matchers.should.Matchers

class UserTest extends AnyFlatSpec with Matchers {
  "User.validateName" should "accept valid names" in {
    User.validateName("John") shouldBe Some("John")
    User.validateName("A".repeat(255)) shouldBe Some("A".repeat(255))
  }

  it should "reject invalid names" in {
    User.validateName("") shouldBe None
    User.validateName("A".repeat(256)) shouldBe None
  }

  "User.validateAge" should "accept valid ages" in {
    User.validateAge(18) shouldBe Some(18)
    User.validateAge(100) shouldBe Some(100)
  }

  it should "reject invalid ages" in {
    User.validateAge(17) shouldBe None
    User.validateAge(101) shouldBe None
  }
}
```

##### **`src/test/scala/com/example/util/UserGeneratorTest.scala`**
```scala
package com.example.util

import org.scalatest.flatspec.AnyFlatSpec
import org.scalatest.matchers.should.Matchers

class UserGeneratorTest extends AnyFlatSpec with Matchers {
  "UserGenerator.generateUser" should "generate a valid user" in {
    val user = UserGenerator.generateUser()
    user.firstName should not be empty
    user.lastName should not be empty
    user.age should (be >= 18 and be <= 100)
    user.position should not be empty
  }

  "UserGenerator.generateUsers" should "generate the correct number of users" in {
    val users = UserGenerator.generateUsers(10)
    users.size shouldBe 10
  }
}
```

---

#### **5. Update Docker Compose**
I’ll update the `docker-compose.yml` to include the necessary services for testing.

##### **`docker-compose.yml`**
```yaml
version: '3.8'
services:
  rest-api-demo:
    build: .
    ports:
      - "8080:8080"
    environment:
      - JAVA_OPTS=-Xmx512m
  mock-server:
    image: wiremock/wiremock:2.35.0
    ports:
      - "8081:8080"
    volumes:
      - ./mock-server:/home/wiremock
  mongodb:
    image: mongo:6.0
    ports:
      - "27017:27017"
    volumes:
      - mongodb_data:/data/db

volumes:
  mongodb_data:
```

---

### **Next Steps**
1. I’ll proceed with **Feature 02: Mocking Service** once you confirm the above implementation.
2. Let me know if you have any feedback or changes!
